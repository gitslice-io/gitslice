interface:
  display_name: "Gitslice Deployment"
  short_description: "Deploy and verify Gitslice staging"
  default_prompt: "Use $deployment to prepare checks, deploy the Gitslice backend or web staging surfaces, and verify agenttools.dev/api.agenttools.dev."
